<div align="center" dir="rtl">
  <img src="logo.svg" alt="لوگو ققنوس" width="200" height="200">
  <h1>ققنوس (Phoenix)</h1>
  <p>
    <img src="https://img.shields.io/badge/License-GPLv2-blue.svg" alt="License">
    <img src="https://img.shields.io/badge/Go-1.21+-00ADD8?logo=go" alt="Go Version">
    <img src="https://img.shields.io/badge/Welcome-PRs-brightgreen.svg" alt="PRs Welcome">
  </p>
  <p>ابزاری قدرتمند برای عبور از فیلترینگ و مقاوم در برابر DPI با استفاده از <strong>HTTP/2 Cleartext (h2c)</strong>.</p>

  [🇬🇧 Read in English 🇬🇧](README.md)
</div>

---

<div dir="rtl">

## 🚀 معرفی

**ققنوس (Phoenix)** نسل جدیدی از ابزارهای تونلینگ است که برای عبور از سیستم‌های پیشرفته بازرسی بسته (DPI) طراحی شده است. این ابزار با استفاده از قدرت **HTTP/2 Cleartext (h2c)** ترافیک را کپسول‌سازی می‌کند و تشخیص آن را برای فایروال‌ها بسیار دشوار می‌سازد، به طوری که عملاً تفاوتی با ترافیک عادی وب ندارد.

برخلاف VPNهای سنتی که پروتکل‌های مشخصی دارند، ققنوس خود را در قالب ترافیک استاندارد وب پنهان می‌کند:
- **مقاوم در برابر حملات ریست (Reset Storms):** اتصال پایدار حتی در شرایط اختلال شدید شبکه و فیلترینگ سنگین.
- **سازگار با CDN:** قابلیت استفاده پشت کلاودفلر (Cloudflare)، Gcore و دیگر CDNهایی که از HTTP/2 پشتیبانی می‌کنند.
- **سربار ناچیز (Zero Overhead):** طراحی شده برای کمترین تأخیر و بالاترین سرعت انتقال داده.

## 🛠 معماری

```mermaid
graph LR
    subgraph Client Side
        A[برنامه‌های کاربر<br/>(تلگرام/مرورگر)] -->|SOCKS5/HTTP| B[کلاینت ققنوس]
    end
    
    B -->|h2c Multiplexed Tunnel| C{اینترنت / CDN / فایروال}
    C -->|h2c Multiplexed Tunnel| D[سرور ققنوس]
    
    subgraph Server Side
        D -->|TCP/UDP| E[مقصد نهایی<br/>(یوتیوب/توییتر/غیر...)]
    end
    
    style B fill:#f9f,stroke:#333,stroke-width:2px
    style D fill:#f9f,stroke:#333,stroke-width:2px
```

## ✅ مزایا و معایب

### مزایا
- **مقاومت در برابر DPI:** مخفی کردن ترافیک درون فریم‌های استاندارد HTTP/2 که تشخیص آن را بسیار سخت می‌کند.
- **پشتیبانی از CDN:** امکان مخفی‌سازی IP سرور اصلی پشت CDNهای بزرگ دنیا.
- **تأخیر پایین:** استفاده از تکنولوژی مالتی‌پلکسینگ برای ارسال همزمان چندین درخواست روی یک اتصال.
- **چندلتفرمی:** فایل اجرایی واحد و سبک برای لینوکس، ویندوز، مک و اندروید (از طریق ترموکس).

### محدودیت‌ها
- **نیاز به HTTP/2:** مسیر شبکه باید از پروتکل HTTP/2 پشتیبانی کند (که اکثر شبکه‌های مدرن پشتیبانی می‌کنند).
- **نیاز به سرور:** برای راه‌اندازی سمت سرور نیاز به یک VPS دارید.

## ⚡ شروع کار

### نصب

شما می‌توانید آخرین نسخه را دانلود کنید یا سورس کد را بیلد بگیرید:

```bash
git clone https://github.com/Selin2005/phoenix.git
cd phoenix
go build -o phoenix-client cmd/client/main.go
go build -o phoenix-server cmd/server/main.go
```

### پیکربندی

**نمونه سمت کلاینت (`example_client.toml`):**
```toml
# آدرس سرور ققنوس
remote_addr = "127.0.0.1:8080"

[[inbounds]]
protocol = "socks5"
local_addr = "127.0.0.1:1080"
enable_udp = true
```

**نمونه سمت سرور (`example_server.toml`):**
```toml
# آدرس گوش دادن سرور
listen_addr = ":8080"

[security]
enable_socks5 = true
enable_udp = true
```

### اجرا

اجرای سرور روی VPS:
```bash
./phoenix-server -c example_server.toml
```

اجرای کلاینت روی سیستم شخصی:
```bash
./phoenix-client -c example_client.toml
```

## ❤️ حمایت و دونیت

اگر این پروژه برای شما مفید بود، می‌توانید برای حمایت از توسعه آن دونیت کنید.

| ارز | آدرس کیف پول |
| :--- | :--- |
| **Ethereum (ETH)** | `0x0000000000000000000000000000000000000000` |
| **Bitcoin (BTC)** | `bc1q00000000000000000000000000000000000000` |
| **USDT (TRC20)** | `T000000000000000000000000000000000` |

</div>

---

<div align="center">
  Made with ❤️ at <a href="https://t.me/FoxFig">FoxFig</a><br>
  Dedicated to all people of Iran 🇮🇷
</div>

## 📄 لایسنس

این پروژه تحت [مجوز GPLv2](LICENSE) منتشر شده است.
